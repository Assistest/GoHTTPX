# GoHTTPX 开发约定

开始修改前阅读 `PROJECT_CONTEXT.md`；测试位置、运行命令和预期变更规则以其中第 14 节为准。

- 正式回归用例必须保存在仓库的约定位置，并由默认测试命令发现；`.tmp/` 只能放临时诊断和生成产物。
- 每次代码修改完成后先跑受影响的测试，再 fresh 执行 Go 全量、race、vet 和 Python 全量回归；安装测试前必须重新构建当前源码的 wheel。
- 既有测试预期默认不变。不能为消除失败而改断言、扩大容差、删除或跳过用例；只有需求明确改变对应行为，或有证据证明用例有误，才可调整，并说明依据。
- 新增功能补正常与边界用例；修复缺陷先补可复现用例。测试失败或未执行时如实报告，不把旧结果算作本次通过。

- 修改 TLS JSON 配置或模板后，必须对实际发出的 ClientHello 核对套件、扩展顺序和参数，并保留固定测试；只确认参数传递或 HTTP 200 不算通过。已有用例位于 `python/test_gohttpx_e2e_transport.py`。

本文件不替代 `PROJECT_CONTEXT.md` 中的协议、状态隔离、资源回收和发布约束。
